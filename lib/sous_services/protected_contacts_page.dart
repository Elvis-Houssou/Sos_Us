import 'package:flutter/material.dart';

class ProtectedContactsPage extends StatelessWidget {
  const ProtectedContactsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Text('proteges'));
  }
}
